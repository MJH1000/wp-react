
import WordPressFeed from "./components/WordPressFeed";

let handleChange = (item: string) => console.log(item);

function App() {
  return (
    <div>
      <WordPressFeed />
    </div>
  );
}

export default App;
